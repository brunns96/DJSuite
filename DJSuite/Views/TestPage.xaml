<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DJSuite.Views.TestPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="{Binding Song.Name, StringFormat='Song Name  - {0:N}'}" FontSize="Medium" />
        <Label Text="{Binding Song.Artist, StringFormat='Song Artist  - {0:N}'}" FontSize="Medium"/>
        <Label Text="{Binding Song.Album, StringFormat='Song Album  - {0:N}'}" FontSize="Medium"/>
    </StackLayout>

</ContentPage>